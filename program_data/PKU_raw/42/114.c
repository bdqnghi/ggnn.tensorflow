//****************************************************** 
//* ? ? ?   ?? ? ? ? ? ? ? ?               *
//* ? ?      ?? ? ?                              *
//* ? ? ? ??2010 ? 10 ? 1 ?                    * 
//* ? ?      ?1000010185                            *
//****************************************************** 
int main()
{
	int n, a[100000], k, count = 0, j = 0; //??a???????count??a?k???
	cin >> n;
	for (int i = 0; i < n; i ++)
		cin >> a[i];
	cin >> k;

	//??j??k?????????????????i?????????k?????????????
	for (int i = 0; i < n; i ++)
		if (a[i] != k)
			a[j ++] = a[i];
		else
			count ++;
	cout << a[0]; //??????????a?????k??????????????k
	for (int i = 1; i < n - count; i ++)
		cout << ' ' << a[i];
	return 0;
}

/* ????????????????????????i????????? */

/* ?IDX(i)????i?????????? */

/* ?OFF(i)????i????????????????? */

/* ????i??????????1??i????? */

/* ???i????????0???i????? */

int main()
{
    int n, i, j;
    unsigned int *isprimebits;    /* ?????????????? */
    scanf("%d", &n);
    
    /* ????????????1??????????? */
    isprimebits = (unsigned int *)
                  malloc((IDX(n) + 1) * sizeof(unsigned int));
    
    /* ????????????????1 */
    memset(isprimebits, -1, (IDX(n) + 1) * sizeof(unsigned int));
    
    /* ????????????????????????? */
    for (i = 3; i <= n/2; i += 2) {
        if (ISPRIME(i)) {
            /* ?? i ????(j)??????????????? */
            for (j = 3 * i; j <= n; j += 2 * i) {
                CLEARPRIME(j);
            }
        }
    }
    
    /* 3?n????????????????????? */
    for (i = 6; i <= n; i += 2) {
        for (j = 3; j <= i / 2; j += 2) {      /* ??????j?i-j */
            if (ISPRIME(j) && ISPRIME(i-j)) {
                printf("%d=%d+%d\n", i, j, i - j);
                break;
            }
        }
    }
    
    free(isprimebits);         /* ???????? */
    return 0;
}


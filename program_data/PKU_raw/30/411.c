/**
 * ?? 1045?7??????
 * ?? ???
 * ?? 1000012844
 */

int main()
{
	int n, i, sum = 0;
	
	cin >> n;					//??n?

	while ( n < 1 )
	{
		cout << " Invalid n, input it again ";	//n < 1???
		cin >> n;
	}
	//??????sum??
	for ( i = 1; i <= n; i++ )
	{
		//???7?????1?7??????7?10??7
		if ( ( i % 7 == 0 ) || ( ( i - 7 ) % 10 == 0 ) 
		|| ( ( i < 80 ) && ( i >= 70 ) ) )
		continue;
		sum = sum + i * i;		// ??
	}

	cout << sum;				//??sum??
	return 0;
}

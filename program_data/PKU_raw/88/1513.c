int main()
{
	char sen[31] , *p = NULL ;					//sen?????????*p???
	int flag = 0 , len;								//flag????len???????
	cin.getline(sen , 31) ;
	len = strlen(sen) ;							//????????
	for (p = sen ; p < sen + len ; p++)			//???????????
	{
		if(*p >= '0' && *p <= '9')				//????????????flag??1
		{
			cout << *p ;
			flag = 1 ;
		}
		if((*p < '0' || *p > '9') && flag == 1)	//??????????????????????????flag??0
		{
			cout << endl ;
			flag = 0 ;
		}
	}
	return 0 ;
}
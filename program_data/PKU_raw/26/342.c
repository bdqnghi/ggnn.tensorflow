/**
 *file:1000012776_5.cpp
 *author : ???1000012776
 *date : 12-12
 *discription : ??????????????
 */
int main ()
{
	char str[100] ;
	char *p , *q ;//?????????
	int flag = 0 ;//?????flag?0?
	cin.getline(str,100) ;
	for ( p = str ; ; p++ )//???????????
	{
		if ( *p == ' ' && flag == 1 )//??????????????????????????
		{
			for ( q = p ; ; q++ )//????????????????
			{
				*q = *(q+1) ;
				if ( *q == '\0' ) break ;
			}
			p-- ;//p???????
		}
		if ( *p == ' ' ) flag = 1 ;//???????????????
		else flag = 0 ;
		if ( *p == '\0' ) break ;
	}
	cout << str << endl ;
	return 0 ;
}